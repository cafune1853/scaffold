<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <dubbo:application name="${dubbo.application.name}" />
    <dubbo:registry address="${dubbo.registry.address}" file="${dubbo.registry.file}"/>

    <dubbo:protocol id="dubbo" name="dubbo"
                    port="${dubbo.provider.port}" />

    <dubbo:service interface="com.doggy.groupId.doggy.module.api.service.TestService" ref="testService"/>
</beans>
